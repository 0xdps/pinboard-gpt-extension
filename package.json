{
  "name": "pingpt-chrome-extension",
  "version": "1.0.1",
  "description": "Pin messages in ChatGPT, add notes, and jump back to them",
  "scripts": {
    "build:icons": "cd icons && node generate-icons.js",
    "build:assets": "cd assets && node generate-assets.js",
    "pack": "zip -r pingpt-v$(node -p \"require('./manifest.json').version\").zip manifest.json background.js content_script_chatgpt.js idb.js popup.html popup.js styles.css icons/*.png -x '*.DS_Store' '*node_modules*'",
    "pack:full": "zip -r pingpt-v$(node -p \"require('./manifest.json').version\")-full.zip manifest.json background.js content_script_chatgpt.js idb.js popup.html popup.js styles.css icons/*.png assets/*.png STORE_COPY.md PRIVACY_JUSTIFICATIONS.md -x '*.DS_Store' '*node_modules*'",
    "build": "npm run build:icons && npm run build:assets",
    "package": "npm run pack",
    "release": "npm run build && npm run pack"
  },
  "dependencies": {
    "canvas": "^3.2.0",
    "sharp": "^0.34.4"
  }
}
